read AGENTS.md for context about this project.

cleanup default next.js boilerplate

setup tailwind css as specified in ui-context.md

move all structure folders into src/ instead of being flat

make sure that the 'src' change is also reflected in configurations like next.js, typescript, eslint, etc.

Do not forget to enforce invariants

Update progress-tracker.md when you are finished